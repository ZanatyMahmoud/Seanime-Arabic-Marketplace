{
  "name": "seanime-arabic-marketplace",
  "version": "0.1.0",
  "private": true,
  "description": "Arabic-enhanced Seanime extension marketplace and provider sources",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "typecheck": "node scripts/typecheck.js",
    "validate": "node scripts/validate-manifests.js && node scripts/validate-marketplace.js && node scripts/static-audit.js",
    "marketplace": "node marketplace/build-marketplace.js",
    "health": "node scripts/health-check.js",
    "test": "npm run build && npm run typecheck && npm run validate"
  },
  "engines": {
    "node": ">=20"
  }
}
